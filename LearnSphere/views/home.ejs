<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to LearnSphere</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes background-pan {
            from { background-position: 0% 0%; }
            to { background-position: -200% 0%; }
        }
        body {
            background: linear-gradient(135deg, #a8c0ff, #3f2b96, #a8c0ff);
            background-size: 400% 400%;
            animation: background-pan 20s ease-in-out infinite;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fadeIn {
            animation: fadeIn 1s ease-out forwards;
        }
        @keyframes buttonGlow {
            0% { box-shadow: 0 0 0 rgba(59, 130, 246, 0.4); }
            50% { box-shadow: 0 0 15px rgba(59, 130, 246, 0.7); }
            100% { box-shadow: 0 0 0 rgba(59, 130, 246, 0.4); }
        }
        .button-glow:hover {
            animation: buttonGlow 1.5s ease-in-out infinite;
        }
    </style>
</head>
<body class="font-sans leading-normal tracking-normal text-white">

    <nav class="bg-white bg-opacity-90 shadow-lg sticky top-0 z-50 backdrop-filter backdrop-blur-lg">
        <div class="container mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <div>
                    <a class="text-2xl font-bold text-gray-800 lg:text-3xl" href="/">LearnSphere</a>
                </div>
                <div class="flex space-x-2 md:space-x-4">
                    <a class="px-4 py-2 text-gray-800 rounded-md hover:bg-gray-200 transition-colors duration-200" href="/auth/login">Login</a>
                    <a class="px-4 py-2 text-white bg-blue-600 rounded-md hover:bg-blue-700 transition-colors duration-200" href="/auth/signup">Sign Up</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-6 py-20 md:py-32 text-center my-10 animate-fadeIn bg-white bg-opacity-20 rounded-lg shadow-xl backdrop-filter backdrop-blur-sm">
        <div class="mx-auto max-w-2xl">
            <h1 class="text-4xl md:text-6xl font-extrabold leading-tight tracking-wide drop-shadow-lg">Welcome to LearnSphere</h1>
            <p class="mt-6 text-lg md:text-xl opacity-90 leading-relaxed drop-shadow-md">The centralized knowledge hub for B.Tech students. Access notes, syllabi, and resources from seniors and faculty, all in one organized place.</p>
            <div class="mt-10 flex flex-col sm:flex-row justify-center gap-4">
                <a href="/auth/login" class="px-8 py-4 bg-white text-blue-600 text-lg font-bold rounded-full hover:bg-gray-100 button-glow transition-all duration-300 transform hover:scale-105">Login</a>
                <a href="/auth/signup" class="px-8 py-4 bg-blue-600 text-white text-lg font-bold rounded-full hover:bg-blue-700 button-glow transition-all duration-300 transform hover:scale-105">Sign Up</a>
            </div>
        </div>
    </div>

    <footer class="bg-white bg-opacity-90 mt-10">
        <div class="container mx-auto px-6 py-4">
            <p class="text-center text-gray-500">© 2024 LearnSphere. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>